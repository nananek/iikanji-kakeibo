{# `body` は呼び出し側で必ず渡す必須パラメータ。
   未指定だと Jinja2 の StrictUndefined / Undefined の挙動次第で
   空行になるか実行時例外になる。

   TODO (Phase 6 後続 PR): フッター文言の運営者名は `MAIL_FROM_NAME`
   等の環境変数経由で組み立てる (運営者依存情報のハードコード回避、
   Phase 1 #66 の方針と整合)。骨格段階のため現状はリテラル。 #}
{{ greeting | default('こんにちは') }}

{{ body }}

---
このメールはいいかんじ™家計簿から送信されています。
心当たりのない場合は破棄してください。
