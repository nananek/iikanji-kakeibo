{
  "type": "module",
  "private": true,
  "description": "Phase E3-C report-side helpers (validator scripts etc). Set type=module so Node can run via ES module imports for tests; browser loads via <script type=\"module\"> independently."
}
